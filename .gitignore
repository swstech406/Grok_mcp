# 编译产物
/grok-search-mcp
/grok-mcp
/bin/
/dist/
/.local-release/
*.so

# Go 测试 / 覆盖率
*.test
*.out
coverage.txt

# SQLite 数据库及 WAL 侧车文件（HTTP 模式本地存储）
*.db
*.db-wal
*.db-shm
*.db-journal

# 本地环境 / 凭证
.env
.env.local

# IDE
.cursor/
.vscode/
.idea/

# 本地工作目录 / 临时草稿
.workbuddy/
.codebase-memory/

# 本机 MCP 客户端配置，含 API key，不应进版本库
.mcp.json

# 管理面板原型页面设计（HTML 原型 / 设计稿）
stitch_mcp_management_panel/

# 本地部署打包产物
*.tar.gz
